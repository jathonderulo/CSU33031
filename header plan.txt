I need: 

2 bytes: header length
3 bytes: Producer / subscriber ID. Will begin with S or P (for sub/prod respectively) and 2 bytes for ID
1 byte for describing content, if its acknowledgement or what. 

header length 2, id 3, packet type (ack/nack/request/frame) 1, payload ?

tcpdump -i eth1 -c 10 -w /compnets/capture.pcap &

docker create -ti --name name_here -v .:/compnets --cap-add=ALL ass1image /bin/bash
docker network connect networkname containername

- implement some sort of objects in broker.py for producers and subscribers
- implement subscribe / unsubscribe
